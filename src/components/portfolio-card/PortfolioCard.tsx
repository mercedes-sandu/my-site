import { StyledPortfolioCard } from "../../styles/PortfolioCard";

function PortfolioCard(title: string, dates: string, description: string, tags: [string]) {
    return (
        <StyledPortfolioCard>
        </StyledPortfolioCard>
    );
}

export default PortfolioCard;